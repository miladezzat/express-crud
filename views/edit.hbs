<form action="/update/{{user._id}}" method="post">
    <input type="text" name="full_name" value="{{user.full_name}}" id="full_name" placeholder="Enter fullname">
    <input type="text" name="phone_number" value="{{user.phone_number}}" id="phone_number" placeholder="Phone number">
    <button type="submit">Save</button>
</form>
